export const Services = (props) => {
  return (
    <div id='services' className='text-center'>
      <div className='container'>
        <div className='section-title'>
          <h2>Key Features</h2>
          <p>
          We provide a calorie tracker, which will help individuals to track their calorie intake, with progress being presented visually on a graph / chart. Also, the database is constantly expanding, with users able to add food that are missing. Furthermore, a forum is provided to allow individuals to help each other and discuss individual plans etc. And many more features are yet to come!
          </p>
        </div>
        <div className='row'>
          {props.data
            ? props.data.map((d, i) => (
                <div key={`${d.name}-${i}`} className='col-md-4'>
                  {' '}
                  <i className={d.icon}></i>
                  <div className='service-desc'>
                    <h3>{d.name}</h3>
                    <p>{d.text}</p>
                  </div>
                </div>
              ))
            : 'loading'}
        </div>
      </div>
    </div>
  )
}
